[build]
command = "hugo -b $URL"
publish = "public/"

[build.environment]
HUGO_VERSION = "0.89.0"
HUGO_ENV = "development"

[context.production.environment]
HUGO_ENV = "production"
command = "make production"

[context.branch-deploy]
command = "hugo -b $DEPLOY_PRIME_URL --buildDrafts -F"

[context.deploy-preview]
command = "hugo -b $DEPLOY_PRIME_URL --buildDrafts -F"

[dev]
command =  "hugo -w --buildDrafts -F "
framework = "#static"
publish = "./public/"
